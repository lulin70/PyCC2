# PyCC2 v0.4.8 推进计划

> **版本**: v0.4.8 | **日期**: 2026-07-06 | **目标**: 推进 P2 技术债 TD-039/TD-040
> **状态**: ✅ 已完成 (2026-07-06) — TD-040 (19 tests) + TD-039 (8 tests) + 版本升级 + 文档同步。技术债 54/64 → 56/64。详见 [CHANGELOG.md](../CHANGELOG.md) v0.4.8 条目。
> **SemVer**: PATCH 递增 (0.4.7 → 0.4.8)，新增向后兼容的错误恢复与健康检查功能

## 一、目标

推进剩余 P2 技术债中的纯功能新增项，提升游戏健壮性：
- **TD-040**: 运行时健康检查 (preflight check) — 启动时检测关键子系统状态
- **TD-039**: 错误恢复机制 — 关键路径 try/except 降级策略

## 二、执行顺序（按风险从低到高）

### P1: TD-040 运行时健康检查 (低风险，新建独立模块)

**文件**:
- 新建 `src/pycc2/infrastructure/diagnostics/__init__.py`
- 新建 `src/pycc2/infrastructure/diagnostics/preflight_check.py`
- 新建 `tests/unit/test_preflight_check.py`
- 修改 `src/pycc2/services/game_loop.py` (集成到 run() 启动前)

**接口设计**:
```python
@dataclass
class PreflightResult:
    ok: bool
    failures: list[str]

def run_preflight_check(game_loop: GameLoop) -> PreflightResult:
    """断言关键子系统 non-None + 可用。"""
    # 检查 renderer/window_manager/event_bus/state/display_config
    # 检查 _combat_director/_render_pipeline (assembler 初始化后)
    # 返回失败列表（空列表 = 全部通过）
```

**集成点**: GameLoop.run() while 循环前调用，失败时 log critical + 返回 1

### P2: TD-039 错误恢复机制 (中风险，修改现有代码)

**文件**:
- 修改 `src/pycc2/services/game_loop_updating.py` (_update_ai 添加 try/except)
- 修改 `src/pycc2/services/game_loop_rendering.py` 或 _render_frame (添加 try/except)
- 新建 `tests/unit/test_error_recovery.py`

**降级策略**:
- AI tick 失败 → log warning + 该单位本 tick 静止（不传播异常）
- Renderer 失败 → log error + 跳过本帧渲染（不传播异常，游戏继续运行）

### P3: 版本升级 + 文档同步 + commit + push

**文件**:
- `VERSION`: 0.4.7 → 0.4.8
- `README.md` / `README_zh.md` / `README_ja.md`: 版本号同步
- `CHANGELOG.md`: 添加 v0.4.8 条目
- `docs/TECH_DEBT.md`: TD-039/TD-040 状态更新 + 总览表同步

## 三、验证标准

- ruff 0 errors
- mypy 0 errors
- pytest 全量通过（零回归）
- 新增测试覆盖 TD-039/TD-040
- integration 181+ passed

## 四、SemVer 评估

TD-039/TD-040 是向后兼容的功能新增（不破坏现有功能），按 SemVer 规则可以是 MINOR 递增 (0.5.0)。但用户明确要求升级到 0.4.8 (PATCH 递增)，按用户要求执行。理由：这些是健壮性增强而非新游戏功能，且不改变任何现有 API。
