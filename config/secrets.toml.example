# PyCC2 Secrets Configuration
# Copy this file to secrets.toml and set your own values
# DO NOT commit real secrets to version control!

# HMAC key for save file integrity verification
# Generate with: python -c "import secrets; print(secrets.token_hex(32))"
hmac_key = "your-random-64-char-hex-key-here"
